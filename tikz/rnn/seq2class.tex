\documentclass[tikz]{standalone}
\usepackage{avanti-color}
\usepackage{avanti-tikz4slides}
\usepackage{avanti-notations}

\tikzset{font=\large}
\tikzset{base/.style={smooth,thick,SolarizedBase01,draw=SolarizedBase01}}
\tikzset{arrow/.style={->,>=stealth,base}}
\tikzset{curve/.style={base,SolarizedBlue}}
\tikzset{nc/.style={base,circle}}
\tikzset{box/.style={base,rectangle,minimum height=0.6cm,minimum width=1.2cm}}
\tikzset{box2/.style={box,fill=SolarizedBase2}}

\begin{document}

\begin{tikzpicture}

    \foreach \i in {2,4,...,16}
        {
            \pgfmathtruncatemacro{\index}{\i/2};
            \pgfmathtruncatemacro{\subscript}{\index};
            \ifthenelse{\i > 8}
            {
                \pgfmathtruncatemacro{\subscript}{\index-4};
            }
            {
                ;
            }
            \ifthenelse{\i=6 \OR \i = 14}
            {
                \node [box,draw=none] (a\index) at (\i,1.5) {...};
                \node [box,draw=none] (x\index) at (\i,0) {...};
            }
            {
                \ifthenelse{\i=8 \OR \i = 16}
                {
                    \node [box2] (y\index) at (\i,3) {$\hat{y}$};
                    \node [box] (a\index) at (\i,1.5) {$\av_T$};
                    \node [box2] (x\index) at (\i,0) {$\xv_T$};
                }
                {
                    \node [box] (a\index) at (\i,1.5) {$\av_\subscript$};
                    \node [box2] (x\index) at (\i,0) {$\xv_\subscript$};
                }
                \draw [arrow] (x\index) -- (a\index);
            }
            \ifthenelse{\i > 2 \AND \NOT 10 = \i}
            {
                \pgfmathtruncatemacro{\im}{\index-1};
                \draw [arrow] (a\im) -- (a\index);
            }
            {
                ;
            }
        }

    \draw [arrow] (a4) -- (y4);

    \node [box] (a) at (12,3) {$\av$};

    \draw [arrow] (a5) -- (a);
    \draw [arrow] (a6) -- (a);
    \draw [arrow] (a8) -- (a);
    \draw [arrow] (a) -- (y8);

\end{tikzpicture}



\end{document}

